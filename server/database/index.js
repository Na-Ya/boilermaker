const db = require('./__db.js');
const User = require('./models/User.js');

//import all models here

//create associations here, this is the table of contents for your db


//export object with db, and models
module.exports = {
    db,
    User
}
