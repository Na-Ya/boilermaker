import { CHANGE_CHANNEL, changeCurrentChannel } from '../actionCreators';

//reducer1
const reducer = (state = {}, action) => {
    switch (action.type) {
      default:
        return state
    }
  }

export default  reducer;
